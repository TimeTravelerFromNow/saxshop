<!DOCTYPE html>
<html>
  <head>
    <title>Steve Gray Saxes</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class='container'>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">

      <div class="container-fluid">
  <a class="navbar-brand" href="<%= root_path %>">Steve Gray's</a>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-md-auto mb-2 mb-lg-0">
      <li class="nav-item ">
        <%= link_to "dashboard", dashboard_index_path, class:" nav-link" %>
      </li>

      <% Category.all.each do |category| %>
      <li class="nav-item " data-controller='dropdown'>
        <a class="nav-link fl-button dropdown-toggle" data-dropdown-target="flButton" data-action='click->dropdown#clicked' role="button" aria-haspopup="true" aria-expanded="false">
          <%= category.title %>
        </a>
        <div class='fl-menu' style='display: none;' data-dropdown-target="flMenu" aria-labelledby="navbarDropdown">
          <% category.makes.each do |make| %>
          <%= link_to make.title, make_path(make), class:'dropdown-item fl-item' %>
          <% end %>
        </div>
      </li>
      <% end %>

    </ul>
  </div>
  </div>
</nav>
    <%= yield %>
    <div class="container">
      <footer class="py-3 my-4">

      <ul class="nav justify-content-center border-bottom pb-3 mb-3">
        <li class="nav-item"> <%= link_to "home", root_path, class:"nav-link px-2 text-muted" %></li>
      </ul>
      <%= link_to "dashboard", dashboard_index_path, class: 'dashboard-btn nav-link active btn btn-primary' %>

      <p class="text-center text-muted">© 2024
        steve gray </p>
    </footer>
    </div>
  </body>
</html>
